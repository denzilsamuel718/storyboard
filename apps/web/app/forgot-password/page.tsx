"use client"; import {FormEvent,useState} from "react"; import Link from "next/link"; import {api} from "@/lib/api";
export default function Forgot(){const[msg,setMsg]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();setError("");setBusy(true);const f=new FormData(e.currentTarget);try{const r=await api<{message:string}>("/auth/forgot-password",{method:"POST",body:JSON.stringify(Object.fromEntries(f))});setMsg(r.message)}catch(cause){setError(cause instanceof Error?cause.message:"Unable to send the reset link")}finally{setBusy(false)}};return <main className="auth-panel" style={{minHeight:"calc(100vh - 44px)"}}><div className="auth-card"><Link href="/" className="brand"><span className="brand-mark">S</span>StoryBoard</Link><h1 style={{marginTop:55}}>Reset access.</h1><p>Enter your email and we’ll prepare a secure reset link.</p>{error&&<div className="alert" role="alert">{error}</div>}{msg?<div className="card" role="status"><h3>Check your inbox</h3><p>{msg}</p><Link className="button secondary" href="/login">Back to sign in</Link></div>:<form onSubmit={submit}><label className="field" htmlFor="reset-email"><span>Email address</span><input id="reset-email" name="email" type="email" autoComplete="email" required/></label><button className="button accent" disabled={busy}>{busy?"Sending…":"Send reset link"}</button></form>}</div></main>}
