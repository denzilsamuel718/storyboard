import Link from "next/link"; import { AuthForm } from "@/components/AuthForm";
export default function Login(){return <main className="auth-layout"><section className="auth-art"><Link href="/" className="brand"><span className="brand-mark">S</span>StoryBoard</Link><blockquote className="auth-quote">“A story finds its shape when someone is ready to see it.”</blockquote><span className="hint">Private creator submission platform</span></section><section className="auth-panel"><div className="auth-card"><div className="eyebrow">Welcome back</div><h1>Continue your story.</h1><p>Sign in to return to drafts, track submissions, and read review updates.</p><AuthForm/><p className="hint" style={{textAlign:"center",marginTop:24}}>New here? <Link href="/register" style={{color:"var(--accent-dark)"}}>Create an account</Link></p></div></section></main>}
