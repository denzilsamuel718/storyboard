"use client";
import { date } from "@/lib/api";
import { useApiData } from "@/lib/useApiData";

export default function Creators(){const{data,error,ready,reload}=useApiData<any>("/admin/creators");const items=data?.creators||[];return <div className="page"><div className="page-head"><div><div className="eyebrow">Community</div><h1>Creators.</h1><p className="page-sub">Account status, verification, contact details, and submission totals.</p></div></div>{error&&<div className="alert" role="alert">{error} <button type="button" className="text-button" onClick={reload}>Try again</button></div>}<div className="table-wrap" aria-busy={!ready}><table><thead><tr><th>Creator</th><th>Contact</th><th>Joined</th><th>Submissions</th><th>Account</th></tr></thead><tbody>{!ready?<tr><td colSpan={5}>Loading creators…</td></tr>:items.length?items.map((creator:any)=><tr key={creator.id}><td><strong>{creator.name}</strong><br/><span className="hint">{creator.emailVerified?"Email verified":"Pending verification"}</span></td><td>{creator.email}<br/><span className="hint">{creator.phone||"No phone"}</span></td><td>{date(creator.createdAt)}</td><td>{creator._count.submissions}</td><td><span className={`status ${creator.active?"ACCEPTED":"NOT_SELECTED"}`}>{creator.active?"Active":"Suspended"}</span></td></tr>):<tr><td colSpan={5}>No creator accounts yet.</td></tr>}</tbody></table></div></div>}
