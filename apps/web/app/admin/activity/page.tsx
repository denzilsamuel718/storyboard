"use client";
import { date } from "@/lib/api";
import { useApiData } from "@/lib/useApiData";

export default function Activity(){const{data,error,ready,reload}=useApiData<any>("/admin/audit");const logs=data?.logs||[];return <div className="page"><div className="page-head"><div><div className="eyebrow">Accountability</div><h1>Activity log.</h1><p className="page-sub">A durable record of important review and account actions.</p></div></div>{error&&<div className="alert" role="alert">{error} <button type="button" className="text-button" onClick={reload}>Try again</button></div>}<div className="table-wrap" aria-busy={!ready}><table><thead><tr><th>Time</th><th>Actor</th><th>Action</th><th>Entity</th><th>Request</th></tr></thead><tbody>{!ready?<tr><td colSpan={5}>Loading activity…</td></tr>:logs.length?logs.map((log:any)=><tr key={log.id}><td>{date(log.createdAt)}</td><td>{log.actor?.name||"System"}<br/><span className="hint">{log.actor?.email}</span></td><td>{log.action.replaceAll("_"," ")}</td><td>{log.entity}<br/><span className="hint">{log.entityId}</span></td><td><span className="hint">{log.requestId||"—"}</span></td></tr>):<tr><td colSpan={5}>No activity has been recorded yet.</td></tr>}</tbody></table></div></div>}
